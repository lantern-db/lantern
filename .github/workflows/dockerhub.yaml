name: Push to Docker registry

on:
  push:
    branches:
      - main

jobs:
  push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Login to Docker Hub
        uses: actions-hub/docker/login@master
        env:
          DOCKER_USERNAME: ${{ secrets.DOCKERHUB_USER }}
          DOCKER_PASSWORD: ${{ secrets.DOCKERHUB_token }}
          DOCKER_REGISTRY_URL: docker.io

      - name: Build
        run: |
          docker build -t lantern .
          docker tag lantern piroyoung/lantern-server:${{ github.sha }}
          docker tag lantern piroyoung/lantern-server:latest-alpha

      - name: Push
        run: |
          docker push piroyoung/lantern-server:${{ github.sha }}
          docker push piroyoung/lantern-server:latest-alpha
